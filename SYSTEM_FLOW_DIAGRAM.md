# 🎨 HRMS System Flow - Visual Guide

## 🔄 Complete System Flow Diagram

```
┌────────────────────────────────────────────────────────────────┐
│                      PHASE 1: COMPANY SETUP                     │
└────────────────────────────────────────────────────────────────┘

Step 1: Company Registration
┌──────────────┐
│   User Form  │
│ Company Info │
│  Admin Info  │
└──────┬───────┘
       │
       ↓ POST /auth/register-company
┌──────────────────────┐
│   Backend Creates:   │
│  1. Company Record   │
│  2. Admin User       │
│     role = "admin"   │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│   Database Tables:   │
│                      │
│  companies           │
│  ├── id: 1          │
│  └── name: "ABC"    │
│                      │
│  users               │
│  ├── id: 1          │
│  ├── company_id: 1  │
│  ├── role: "admin"  │
│  └── username: "...│
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│  Response to User:   │
│  "Company Created!"  │
│  "Admin: johndoe"    │
└──────────────────────┘


┌────────────────────────────────────────────────────────────────┐
│                    PHASE 2: ADMIN OPERATIONS                    │
└────────────────────────────────────────────────────────────────┘

Step 2: Admin Login
┌──────────────┐
│  Admin User  │
│  Enters:     │
│  - Username  │
│  - Password  │
└──────┬───────┘
       │
       ↓ POST /auth/login
┌──────────────────────┐
│  Backend Verifies:   │
│  1. Username exists  │
│  2. Password correct │
│  3. User active      │
│  4. Company active   │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│  Creates JWT Token:  │
│  {                   │
│    user_id: 1,       │
│    company_id: 1,    │
│    role: "admin"     │
│  }                   │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│  Returns:            │
│  - Token             │
│  - User Info         │
└──────────────────────┘


Step 3: Admin Creates Department
┌──────────────┐
│ Admin sends  │
│ with token:  │
│ "Engineering"│
└──────┬───────┘
       │
       ↓ POST /departments (with token)
┌──────────────────────┐
│  Backend:            │
│  1. Verify token     │
│  2. Check role=admin │
│  3. Create dept      │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│  Database:           │
│  departments         │
│  ├── id: 1          │
│  ├── company_id: 1  │
│  └── name: "Eng"    │
└──────────────────────┘


Step 4: Admin Adds Employee
┌──────────────┐
│ Admin sends  │
│ Employee     │
│ Details +    │
│ Initial Pass │
└──────┬───────┘
       │
       ↓ POST /employees (with admin token)
┌──────────────────────┐
│  Backend Creates:    │
│  1. Employee record  │
│  2. User account     │
│     role = "employee"│
│     force_pwd = true │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────────┐
│  Database:               │
│                          │
│  employees               │
│  ├── id: 1              │
│  ├── company_id: 1      │
│  ├── dept_id: 1         │
│  └── name: "John"       │
│                          │
│  users                   │
│  ├── id: 2              │
│  ├── company_id: 1      │
│  ├── employee_id: 1     │
│  ├── role: "employee"   │
│  ├── username: "emp001" │
│  └── force_pwd: true    │
└──────┬───────────────────┘
       │
       ↓
┌──────────────────────┐
│  Response:           │
│  username: "emp001"  │
│  temp_pwd: "temp123" │
└──────────────────────┘


┌────────────────────────────────────────────────────────────────┐
│                  PHASE 3: EMPLOYEE OPERATIONS                   │
└────────────────────────────────────────────────────────────────┘

Step 5: Employee First Login
┌──────────────┐
│  Employee    │
│  Enters:     │
│  - emp001    │
│  - temp123   │
└──────┬───────┘
       │
       ↓ POST /auth/login
┌──────────────────────┐
│  Backend Verifies    │
│  & Returns Token     │
│  + User Info         │
│  force_pwd: true ← ! │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│  Frontend sees:      │
│  force_pwd = true    │
│  Shows: "Change      │
│  Password Dialog"    │
└──────────────────────┘


Step 6: Employee Changes Password
┌──────────────┐
│  Employee    │
│  Submits:    │
│  - Current   │
│  - New       │
│  - Confirm   │
└──────┬───────┘
       │
       ↓ POST /auth/change-password (with token)
┌──────────────────────┐
│  Backend:            │
│  1. Verify current   │
│  2. Update password  │
│  3. Set force_pwd    │
│     = false          │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│  Database Updated:   │
│  users               │
│  └── force_pwd:false │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│  Success!            │
│  Employee can now    │
│  access portal       │
└──────────────────────┘


Step 7: Employee Portal Access
┌──────────────┐
│  Employee    │
│  Logs in     │
│  with new    │
│  password    │
└──────┬───────┘
       │
       ↓ Normal login flow
┌──────────────────────┐
│  Gets Token          │
│  force_pwd: false    │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│  Can Access:         │
│  - View profile      │
│  - View colleagues   │
│  - Change password   │
│  - View attendance   │
│  - View payroll      │
└──────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────────────────────┐
│                     LOGIN PROCESS                            │
└─────────────────────────────────────────────────────────────┘

User Input: username + password
         ↓
    ┌─────────────┐
    │  Backend    │
    │  Receives   │
    └──────┬──────┘
           │
           ↓
    ┌─────────────────────┐
    │  Step 1: Find User  │
    │  by username        │
    └──────┬──────────────┘
           │
           ├─ Not Found → 401 "Invalid credentials"
           │
           ↓ Found
    ┌─────────────────────┐
    │  Step 2: Verify     │
    │  Password Hash      │
    └──────┬──────────────┘
           │
           ├─ Wrong → 401 "Invalid credentials"
           │
           ↓ Correct
    ┌─────────────────────┐
    │  Step 3: Check      │
    │  user.is_active     │
    └──────┬──────────────┘
           │
           ├─ False → 403 "Inactive account"
           │
           ↓ True
    ┌─────────────────────┐
    │  Step 4: Check      │
    │  company.is_active  │
    └──────┬──────────────┘
           │
           ├─ False → 403 "Company inactive"
           │
           ↓ True
    ┌─────────────────────┐
    │  Step 5: Create     │
    │  JWT Token          │
    │  {                  │
    │    sub: username,   │
    │    user_id: ...,    │
    │    company_id: ..., │
    │    role: ...,       │
    │    exp: ...         │
    │  }                  │
    └──────┬──────────────┘
           │
           ↓
    ┌─────────────────────┐
    │  Return:            │
    │  - access_token     │
    │  - user info        │
    │  - role             │
    └─────────────────────┘
```

---

## 🛡️ Authorization Flow (Protected Endpoints)

```
┌─────────────────────────────────────────────────────────────┐
│              ACCESSING PROTECTED ENDPOINT                    │
└─────────────────────────────────────────────────────────────┘

User Request: GET /employees
Headers: Authorization: Bearer [token]
         ↓
    ┌─────────────────────┐
    │  Dependency:        │
    │  get_current_user() │
    └──────┬──────────────┘
           │
           ↓
    ┌─────────────────────┐
    │  Extract token      │
    │  from header        │
    └──────┬──────────────┘
           │
           ↓
    ┌─────────────────────┐
    │  Decode JWT token   │
    └──────┬──────────────┘
           │
           ├─ Invalid/Expired → 401 Unauthorized
           │
           ↓ Valid
    ┌─────────────────────┐
    │  Get user from DB   │
    │  using token data   │
    └──────┬──────────────┘
           │
           ├─ Not found → 401 "User not found"
           │
           ↓ Found
    ┌─────────────────────┐
    │  Check user active  │
    │  Check company      │
    │  active             │
    └──────┬──────────────┘
           │
           ├─ Inactive → 403 Forbidden
           │
           ↓ Active
    ┌─────────────────────┐
    │  For Admin routes:  │
    │  require_admin()    │
    └──────┬──────────────┘
           │
           ├─ Not admin → 403 "Admin required"
           │
           ↓ Is admin
    ┌─────────────────────┐
    │  Execute route      │
    │  handler            │
    └──────┬──────────────┘
           │
           ↓
    ┌─────────────────────┐
    │  Return response    │
    │  with data          │
    └─────────────────────┘
```

---

## 🏢 Multi-Tenant Isolation

```
┌─────────────────────────────────────────────────────────────┐
│              HOW COMPANY ISOLATION WORKS                     │
└─────────────────────────────────────────────────────────────┘

Database State:

companies
├── id: 1, name: "Tech Corp"
└── id: 2, name: "Biz Inc"

users
├── id: 1, company_id: 1, role: "admin", username: "tech_admin"
├── id: 2, company_id: 1, role: "employee", username: "tech_emp"
├── id: 3, company_id: 2, role: "admin", username: "biz_admin"
└── id: 4, company_id: 2, role: "employee", username: "biz_emp"

employees
├── id: 1, company_id: 1, name: "John (Tech Corp)"
├── id: 2, company_id: 1, name: "Jane (Tech Corp)"
├── id: 3, company_id: 2, name: "Bob (Biz Inc)"
└── id: 4, company_id: 2, name: "Alice (Biz Inc)"


Scenario: tech_admin requests all employees
         ↓
    Token contains: { company_id: 1 }
         ↓
    Query: SELECT * FROM employees
           WHERE company_id = 1  ← Automatic filter!
         ↓
    Returns: [John, Jane]  ← Only Tech Corp employees
         ↓
    Bob and Alice are NEVER visible to Tech Corp users!


This happens automatically in every query:
- EmployeeService.get_all_employees(company_id)
- Service layer enforces company_id filter
- No way for Company A to see Company B's data
```

---

## 📊 Data Relationships

```
┌────────────────────────────────────────────────────────┐
│                 DATABASE RELATIONSHIPS                  │
└────────────────────────────────────────────────────────┘

Company (Tenant)
│
├─► Users (Admin & Employees)
│   │
│   └─► One admin per company (can be multiple)
│   └─► Multiple employees linked to Employee records
│
├─► Departments
│   │
│   └─► Multiple departments per company
│
└─► Employees
    │
    ├─► Linked to Department
    └─► Has User account (for login)


Visual:

     ┌──────────────┐
     │   Company    │
     │   (id: 1)    │
     └───────┬──────┘
             │
      ┌──────┴──────┬───────────────┬──────────────┐
      │             │               │              │
┌─────▼─────┐ ┌────▼─────┐  ┌──────▼──────┐ ┌────▼─────┐
│Admin User │ │Department│  │Department   │ │Employee  │
│(role:admin│ │(Eng)     │  │(HR)         │ │Records   │
└───────────┘ └──────────┘  └─────────────┘ └────┬─────┘
                                                  │
                                            ┌─────▼─────┐
                                            │User       │
                                            │Accounts   │
                                            │(role:emp) │
                                            └───────────┘
```

---

## 🎯 Role-Based Access Matrix

```
┌───────────────────────────────────────────────────────────┐
│              WHO CAN DO WHAT?                             │
└───────────────────────────────────────────────────────────┘

Action                      │  Admin  │  Employee  │  Public
─────────────────────────────┼─────────┼────────────┼─────────
Register Company            │    ✗    │     ✗      │    ✓
Login                       │    ✓    │     ✓      │    ✓
Change Own Password         │    ✓    │     ✓      │    ✗
View Own Profile            │    ✓    │     ✓      │    ✗
─────────────────────────────┼─────────┼────────────┼─────────
Create Department           │    ✓    │     ✗      │    ✗
View Departments            │    ✓    │     ✓      │    ✗
Update Department           │    ✓    │     ✗      │    ✗
Delete Department           │    ✓    │     ✗      │    ✗
─────────────────────────────┼─────────┼────────────┼─────────
Create Employee             │    ✓    │     ✗      │    ✗
View All Employees          │    ✓    │     ✓      │    ✗
View Employee Details       │    ✓    │     ✓      │    ✗
Update Employee             │    ✓    │     ✗      │    ✗
Delete Employee             │    ✓    │     ✗      │    ✗
─────────────────────────────┴─────────┴────────────┴─────────

Note: All data is isolated by company_id
      Admin of Company A cannot manage Company B's data
```

---

## 📝 Request/Response Examples

### **Company Registration**
```
Request:
POST /api/v1/auth/register-company
{
  "company_name": "Tech Innovations",
  "company_email": "admin@techinnovations.com",
  "admin_username": "admin",
  "admin_password": "SecurePass123"
}

                    ↓

Database Changes:
INSERT INTO companies VALUES (1, 'Tech Innovations', ...)
INSERT INTO users VALUES (1, 1, 'admin', 'admin@...', 'admin', ...)

                    ↓

Response:
{
  "company": { "id": 1, "company_name": "Tech Innovations", ... },
  "admin_username": "admin",
  "message": "Company registered successfully!"
}
```

### **Employee Creation**
```
Request:
POST /api/v1/employees
Authorization: Bearer [admin_token]
{
  "employee_code": "EMP001",
  "first_name": "John",
  "last_name": "Doe",
  "email": "john@techinnovations.com",
  "hire_date": "2025-01-15",
  "initial_password": "Welcome2025"
}

                    ↓

Token Decoded:
{ user_id: 1, company_id: 1, role: "admin" }

                    ↓

Database Changes:
INSERT INTO employees VALUES (1, 1, 'EMP001', 'John', 'Doe', ...)
INSERT INTO users VALUES (2, 1, 'emp001', ..., 'employee', 1, true)
                                              ↑           ↑    ↑
                                           role    emp_id  force_pwd

                    ↓

Response:
{
  "employee": { "id": 1, "full_name": "John Doe", ... },
  "username": "emp001",
  "temp_password": "Welcome2025",
  "message": "Employee created successfully."
}
```

---

**For complete implementation details, see:**
- 📘 `BEGINNER_GUIDE.md` - Detailed explanations
- 📗 `IMPLEMENTATION_GUIDE.md` - Technical documentation
- 🚀 `QUICK_START.md` - Get started in 5 minutes
- 📋 `CHANGES_SUMMARY.md` - What changed

---

*Created: October 30, 2025*
*HRMS Multi-Tenant System v2.0*

